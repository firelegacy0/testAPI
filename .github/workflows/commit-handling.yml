name: Commit Handling

on:
  push:
    branches:
      - master

jobs:
  test-commit-message:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set Env Var from Commit Message
        run: |
          update_description=$(printf '%s\n' "${{ github.event.head_commit.message }}" | head -1)
          echo "UPDATE_DESCRIPTION=${update_description}" >> $GITHUB_ENV
      - name: Display Commit Message
        run: |
          echo "Commit Message: ${{ env.UPDATE_DESCRIPTION }}"
          echo "Simulating syntax error"
          eval "echo ${UPDATE_DESCRIPTION}"
